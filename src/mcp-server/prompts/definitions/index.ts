/**
 * @fileoverview Barrel export for all prompt definitions. No prompts defined per design spec.
 * @module mcp-server/prompts/definitions/index
 */

export const allPromptDefinitions: never[] = [];
